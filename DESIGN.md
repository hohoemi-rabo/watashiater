# ワタシアター DESIGN.md

**バージョン**: 1.2（2026-08-15 拡大表示は写真を切り抜かない、に改訂。1.1 = 2026-08-14 チケット28：背景を桜〜ラベンダー〜空色の3色に刷新・影を紫に変更）
**作成日**: 2026-08-03
**対象**: Claude Code 実装引き継ぎ用（REQUIREMENTS.md v1.0 とセット）

---

## 1. デザインコンセプト：「マチネ（昼公演）」

**日差しの入る、昼の明るい劇場。**

夜の重厚な劇場（黒・臙脂・金）ではない。開演前のロビーに光が差していて、
これから「わたし」の上映が始まる——その明るさと高揚感を作る。

### 3つのキーワード

1. **浮かんでいる**：桜色〜ラベンダー〜空色の背景の上に、白いカードが影を落として浮かぶ。フラットデザイン禁止
2. **手ざわりがある**：木の机、紙のカード、ポラロイドの白フチ。デジタルの画面に物の質感を持ち込む
3. **色をけちらない**：単色アクセントの禁欲的デザインにしない。幕の朱・スポットの黄・木の茶・桜とラベンダーの空が同居する

### 参照と決別

- 参照元：Cloubo の「空グラデ背景＋白カード＋影」の骨格
- **決別点：天使・雲の写真素材は使わない**。空はグラデーションと抽象的な形だけで表現する
- 決別点2：葬送・追悼の気配（白菊、灰色、燭台的な演出）を一切入れない

---

## 2. デザイン原則（実装判断の基準）

1. **すべてのカードは浮いている**：影のない・枠線だけのカードを作らない（§5 影スケール必須）
2. **完全な水平垂直を疑う**：写真・装飾要素には ±1〜3° の傾きを許す。整いすぎ＝命のなさ
3. **1画面に最低2色**：背景＋白＋アクセント1色で終わらせない。ただし1画面のアクセントは2色まで
4. **シニア対応は「見えないところ」でやる**：読みやすさは文字の巨大化ではなく、
   タップターゲット・余白・コントラスト・短い文で担保する。
   **文字サイズ変更機能は実装しない**。設定項目・アクセシビリティメニューでシニア感を演出しない
5. **迷ったら明るい方**：彩度・明度で二択に迷ったら、明るく楽しい方を選ぶ

---

## 3. カラーパレット

### 基本トークン

| トークン | 値 | 用途 |
|---|---|---|
| `sky-top` | `#FFD6E8` | 背景グラデ上端（桜色） |
| `sky-mid` | `#E0D4FF` | 背景グラデ中間 60%（ラベンダー） |
| `sky-bottom` | `#C4E8FF` | 背景グラデ下端（淡い空色） |
| `card-white` | `#FFFFFF` | カード面 |
| `curtain-red` | `#E0472F` | 幕の朱色。主役ボタン・進行中バッジ・ロゴ |
| `spot-yellow` | `#F5B93C` | スポットライトの黄。見たよ（拍手）・達成・ハイライト |
| `desk-wood` | `#C99A68` | 机の上ボードの木肌（グラデ・木目テクスチャ可） |
| `stage-navy` | `#2B3A55` | 本文テキスト。真っ黒(#000)は使わない |
| `text-soft` | `#6B7280` | 補助テキスト |
| `curtain-gold` | `#E3AD4E` | **補助色**。自分史の緞帳の縁と房だけに使う（2026-09-08 追加。`spot-yellow` 60% + `desk-wood` 40% の混色） |
| `line-green` | `#06A845` | **補助色**。「LINEで送る」ボタンだけに使う（2026-09-12 追加。LINE 公式色 `#06C755` を少し落とした値＝白文字とのコントラストを 2.2:1 → 3.1:1 にして大きな太字の基準を満たすため） |

### 使い方のルール

- 背景は常に `sky-top → sky-mid(60%) → sky-bottom` の縦3色グラデーション（全画面共通。閲覧Webも同じ。2026-08-14 刷新）
- `curtain-red` は各画面で最も重要なアクション1つにだけ使う
- `spot-yellow` はリアクション・お祝い・進捗の色。エラーには使わない
- エラー・削除系は `#C0392B` 系の落ち着いた赤（curtain-red と区別する）
- 機能色（画面ごとのテーマ色）を増やしたい場合も、この基本9トークンの範囲で組み合わせる
- 補助色（`error-red` / `story-paper`＝§4 / `shadow-plum`＝§5 / `curtain-gold` / `line-green`）は用途を1つに限った例外。増やすときは必ず既存トークンの混色として値を決め、用途を本書に書く

---

## 4. タイポグラフィ

| 役割 | フォント | 備考 |
|---|---|---|
| 見出し・ロゴ・ボタン | **Zen Maru Gothic**（Bold） | 丸みのある太字。劇場の看板文字のイメージ |
| 本文・UI | **Noto Sans JP** | Regular / Medium |
| **自分史ページのみ** | **Shippori Mincho** | 読み物としての風格。ここだけ明朝に切り替える |

### サイズ（アプリとWebで別スケール。変更機能なし）

スマホアプリで大きすぎる文字は1行の文字数が減り、バランスが崩れる。
**アプリは標準的なサイズ、閲覧Webはゆったり大きめ**、と役割で分ける。

| 用途 | アプリ（Expo） | 閲覧Web |
|---|---|---|
| 画面タイトル | 24px / Bold | 28px / Bold |
| カードタイトル | 18px / Bold | 20px / Bold |
| 本文 | 16px / Regular（**14px未満を本文に使わない**） | 18px / Regular |
| 補助・キャプション | 13px | 14px |
| ボタンラベル | 16px / Bold | 18px / Bold |

- 行間は本文 1.7（アプリ）／1.8（Web）。自分史ページはどちらも 2.0
- **自分史ページは例外**：読み物なのでアプリ側も本文 18px まで上げてよい
- 文字を標準サイズに抑える分、シニア対応は「タップターゲット48dp以上」「余白」「コントラスト」で担保する（サイズだけが読みやすさではない）
- 自分史は、空グラデの上に生成り（`#FBF7EF`）の**「本のページ」カード**を浮かべて「一冊の本」の空気を作る（2026-08-14 改訂：紙色ベタの全画面から変更）。ページは扉（中央の題字＋ニックネーム＋木色の飾り罫）→ 全角1字下げの段落 → 右寄せの奥付（作成日）の体裁。枠罫と飾り罫は `desk-wood` の細線

---

## 5. 立体感の実装仕様（本書の心臓部）

### 影スケール（3段階。これ以外の影を発明しない）

| レベル | 用途 | 値の目安 |
|---|---|---|
| `shadow-rest` | 置かれているカード・写真 | y=2px blur=8px rgba(140,60,180,0.12) |
| `shadow-raised` | ボタン・タップ可能カード | y=4px blur=14px rgba(140,60,180,0.16) |
| `shadow-lifted` | ドラッグ中の写真・モーダル | y=10px blur=28px rgba(140,60,180,0.24) |

- 影色は `shadow-plum`（`#8C3CB4`）の紫（2026-08-14 刷新：桜系の背景に stage-navy の青い影は濁って見えるため。不透明度・オフセットは従来のまま）

- Expo側は elevation との対応を取り、Android実機で必ず目視確認する
- タップ時はカードがわずかに沈む（scale 0.98 ＋ shadow-rest へ）。「押した感」を必ず作る
- **補助ボタンには文字と同じ色の2px枠を回す**（2026-09-12 実機でユーザー指摘）。ボタンの地 `card-white` とカードの地（`card-white` をわずかに桜色へ寄せた紙色）はほぼ同じ色で、カードの上に置くと影だけが頼りになり「押せるもの」に見えなかった。主役ボタン（`curtain-red`）とLINEボタン（`line-green`）は地が濃いので枠は不要

### 質感

- **ポラロイド**：写真は白フチ（上下左右 8px、下だけ 28px）＋ `shadow-rest` ＋ ±3°以内のランダム傾き。下部余白にキャプション（お題タイトル）。手書き風フォントは**使わない**（読みにくい）。Zen Maru Gothic 14px
- **机の上ボード**：`desk-wood` の背景に、ごく薄い木目（軽量テクスチャ画像1枚。`app/scripts/gen-wood-tile.mjs` で生成した年輪入りタイル）。外周の「机の縁」（内側シャドウ・木枠）は**作らない**——チケット13の実機確認で「枠が何か分からない」ため削除した（2026-08-12 ユーザー判断。閲覧Web も同様）
- **カード紙質**：白カードには 1px の極薄ハイライト（上辺）を入れ、紙のエッジを感じさせる。ベタの白矩形にしない

---

## 6. シグネチャー要素（この2つがワタシアターの顔）

### 6.1 開幕（閲覧Webの表紙）

孫がURLを開いた瞬間、**朱色の緞帳が左右に開いて表紙（名前と代表写真）が現れる**。
1回きり・1.2秒・スキップ可（タップで即開演）。`prefers-reduced-motion` 時は最初から開いた状態。
アプリ側では使わない（毎回見せられると鬱陶しい）。**閲覧Webの初回訪問だけの演出**とする。

### 6.2 演目札（お題カード）

お題一覧のカードは、劇場の演目札／チケット半券のメタファー。

- 左端に半券のミシン目（点線＋半円の切り欠き）
- 未回答：白地。回答済み：切り欠き部に `spot-yellow` の「済」スタンプ風マーク＋写真サムネイル
- 進捗表示は「10このうち4つ答えました」のテキスト＋座席が埋まるように点灯するドット10個

---

### ロゴ表記（2026-08-15 ユーザー決定）

実体は `app/components/app-logo.tsx`（ホームと onboarding で共有）。

- 表記は「ワタシアター」のまま。**「ワタシ」と「シアター」が重なる「シ」の一字だけを `curtain-red`** にし、他の字は `stage-navy`。合成語の由来を、読みを崩さずに見せるため（読み手が60〜80代・ストア掲載名も同じ表記なので、ひらがな混ぜの表記は採らない）
- 下段に `WATASHI THEATER`（Zen Maru Gothic・10px・字間 3.4）。REQUIREMENTS §1.1 が認める「ロゴ装飾としての英字」の範囲。劇場のプログラムらしい佇まいを作る要素なので、§4「14px未満を本文に使わない」の対象外
- 全字を朱色にしていた旧版には戻さない

## 7. 画面別ガイド

### ホーム
- 上部：ロゴ（`components/app-logo.tsx`。下記のロゴ表記に従う）
- 中央：進捗（演目札ドット）と「お題を書く」ボタン（curtain-red）
- 下部：ギャラリー（机の上）への入り口を、木目のミニプレビューで見せる

### お題一覧
- 演目札カードの縦積み。自由お題枠は最後に「＋じぶんでお題をつくる」

### 回答画面
- 1画面1お題。お題タイトルを舞台の題字のように大きく
- テキスト⇄音声の切替は大きな2ボタン（アイコン＋文字）。録音中は `spot-yellow` の波形アニメ
- 写真添付枠はポラロイド型の点線の空枠。**写真の末尾に常に出し**（5枚に達したら消す）、**この枠自体が写真を選ぶ入り口＝押せる**（2026-09-12 クローズドテストの声。生徒さんはボタンではなく枠を押そうとした）。「写真をのせる」ボタンも残して入り口は2つ。読み込み中・アップロード中・オフライン・読み込み失敗のときは薄くして押せなくする

### ギャラリー（机の上）
- **画面名は「ギャラリー」**（画面タイトル・ホームの入り口・家族向け画面・閲覧Webの見出しすべて）。「机の上」は木の机に写真を並べるという**見た目の比喩の呼び名**で、UI には出さない（2026-08-15 ユーザー判断：スマホで「ギャラリー」は通りが良く説明が要らない）
- 全面 `desk-wood`。ポラロイドが自動整列（傾き・軽い重なりあり）
- **写真をタップしたときの見た目は変えない**（拡大表示がすぐ開くこと自体が反応。半透明にすると木目が透けて写真が汚れて見えた。2026-08-15 ユーザー指摘で削除）
- 「並べ替え」中はドラッグ対象が `shadow-lifted` で浮き上がる
- 音声付き写真の右下に小さな `spot-yellow` のスピーカーバッジ
- 写真タップ→拡大：背景を暗くせず、**背景色（桜色）を少し濃くする**程度（夜にしない）
- **開き方は「ひだのある朱色の幕が左右に開いて、その奥から写真が現れる」**（2026-09-12 改訂。幕が束ねられながら開く 600ms → 320ms 後に写真が下からすっと現れる 280ms）。濃くした背景色の面は**幕ではなく舞台の地**なので動かさない（旧版はこの面が scaleX で広がるだけで、320ms では動きが読めなかった＝クローズドテストの声）。幕は自分史の緞帳とひだ・束ね縮みを共有するが、**金の縁と飾り幕は付けず 0.6秒で開く**（写真とボタンにかぶるうえ、写真を見るたびに毎回出るので豪華すぎると邪魔になる）。開き切ったら幕は消す
- 拡大表示は写真を**切り抜かず、実際の縦横比で全体を見せる**（縦写真の頭と足が切れていた不具合の修正。2026-08-15 ユーザー決定）。正方形の切り抜きは机の上・回答画面プレビューのポラロイドだけ

### 自分史
- §4の通り「空に浮かぶ本のページ」＋明朝（扉・1字下げ・奥付）。生成中は緞帳が閉じて「じゅんびちゅう…」→開いて本文（**朱色の緞帳そのもの**を出すのはアプリ内でここだけ）。閲覧Webの自分史の段も同じ体裁
- 緞帳の姿（2026-09-08 クローズドテストの声で改訂）：平らな赤い板ではなく**布の緞帳**にする。縦のひだ（明暗の縦縞）／内側（中央側）の端に細い `curtain-gold` のライン／画面上端に弧を並べた飾り幕と短い房。開くときは左右へ退場するのではなく**外側の端へ束ねられながら幅が縮む**（ひだが詰まる）。飾り幕は開幕の終盤でフェードアウトし、本文の上に残さない

### みんなに見せる／せってい
- 装飾は最小限でよいが、影スケールと余白は他画面と同一に保つ
- 送る導線は2本立て（2026-09-12 クローズドテストの声）：**「LINEで送る」（`line-green` の主役級ボタン。LINE を直接開く）** と、その下の「ほかの方法で送る」（共有シート。LINE を使わない家族向け）。招待コード・見せる用リンクの両方に同じ並びで置く。**LINE のロゴ画像は使わない**（商標ガイドラインの制約を持ち込まない）＝緑地に白文字と汎用の吹き出しアイコンで表す。この画面のアクセントは `curtain-red`（招待コードをつくる）＋ `line-green` の2色＝§11-6 の範囲内

### 閲覧Web（/w/[slug]）
- 開幕演出（§6.1）→ 表紙 → 机の上（閲覧のみ・配置再現）→ 自分史 → 演目札一覧
- 縦に長いので、幕が開いたあと画面下に**「下にもあります」の案内**を出す（2026-09-12 クローズドテストの声）。`stage-navy` の丸（64px）に白い指（lucide `Pointer`）＋ `shadow-raised`、下に `stage-navy` の文字。指が下へスワイプするしぐさを**3回だけ**繰り返して止まり、そのあとは静かに残る。押すと1画面ぶん近く下へ動く。**消えるのはページの終わりが見えてから**（残りが画面の高さの6割を切ったとき＝一番下のお題が見えるあたり。ページの長さは写真や回答の数で変わるので固定 px では測らない）。一度消したら出し直さない。色を濃くしたのは白いカードに重なっても読めるようにするため（3案を実機で見くらべて決定）
- アプリとトークン完全共有。Webだけ雰囲気が変わることを禁止

---

## 8. モーション

- 基本 duration 200ms / ease-out。装飾アニメの常時ループ禁止。**唯一の例外は閲覧Webの「下にもあります」の指**で、回数を3回に切って止める（2026-09-12。しぐさを伝えるには繰り返しが要るが、ページが延々とちらつくのは避ける）
- 使ってよい演出：開幕（§6.1）、見たよ＝**拍手**（タップで手のひらアイコンが1回はじけ、`spot-yellow` の小さな紙吹雪 0.6秒）、ドラッグの浮き上がり、保存成功のスタンプ
- reduced-motion 設定を全演出で尊重
- 基準 200ms の例外は幕だけ：**自分史の緞帳は閉幕1.0秒・開幕2.0秒**、**ギャラリーの拡大表示の幕は0.6秒**（どちらも 2026-09-08 クローズドテストの声で延長。拡大表示が短いのは写真を見るたびに毎回出るため）。閲覧Webの開幕（§6.1）は訪問者を待たせない演出なので**1.2秒のまま**＝3つはそろえない

---

## 9. アイコン・イラスト

- アイコン：線幅2pxのラウンドライン系で統一（lucide 系のトーン）。塗りつぶしは選択状態のみ
- イラストを使う場合は、幾何学的な切り絵風（フラットな色面＋影1枚）。写真素材のクリップアート・天使・雲の実写は禁止
- 絵文字をUIに使わない

---

## 10. 文言トーン（DESIGN側の追加分）

- REQUIREMENTS §4.1 のやさしい日本語原則を継承
- 劇場語彙は**ほんのり**使う：「開幕」「演目」はOK。「公演終了」「閉幕」「千秋楽」など終わりを連想させる劇場語は禁止
- ボタンは動詞で：「作る」「のせる」「並べ替え」「見せる用リンクを作る」

### 表記ルール（2026-09-12 クローズドテストの声で確定。ここが唯一の情報源）

ひらがなの分かち書きは 60〜80代にはかえって読みにくい、という声を受けて全面的に改めた。

- **日常的な漢字はスペースなしで使う**：写真・家族・声・思い出・答える・書く・読み込む・作る・送る・
  見る・設定・保存・準備中・確かめる・消す・閉じる・登録・電波・必要 など
- **語と語の間のスペース（分かち書き）は使わない**。区切りは読点「、」と短い文で作る
- **送り仮名は標準表記**：「答える」「作る」「読み込む」「書き直す」
- **外来語・専門語は避ける**（アップロード→のせる、生成→作る。REQUIREMENTS §4.1）
- **難しい漢字は避ける**：「失効」→「使えなくなる」、「認証」→「ログイン」
- **丁寧体を保つ**（〜ください／〜できます）。命令調にしない
- ひらがなのままにする語：**のせる・やめる・わかりました・つながる・ほかの**
  （単語として平易で、漢字にすると硬くなる）
- 固定文言：**「お題を書く」「自分史を作る」「見たよ」「並べ替え」「みんなに見せる」**

---

## 11. 禁止事項（まとめ）

1. 天使・雲・空の**写真素材**（グラデと抽象形のみで空を表現）
2. 影なしのフラットデザイン／枠線だけのカード
3. 文字サイズ変更機能・アクセシビリティ設定メニュー
4. 黒背景・夜の劇場化（拡大表示含む）
5. 白菊・灰色基調・葬送の気配、「閉幕」系の語彙
6. 1画面に3色以上のアクセント色
7. 絵文字のUI利用、手書き風フォント

---

## 12. Claude Code への注意

- 本書のトークン（色8・影3・フォント3・サイズ5）を定数ファイルに定義し、生値のハードコードをしない
- Expo と Next.js でトークン定義を二重管理する場合、値の一致をコメントで相互参照する
- 実装中に本書にない見た目の判断が必要になったら、§2 の原則1〜5で判断し、判断内容をコードコメントに残す
