# ワタシアター REQUIREMENTS-PHASE2.md（上映会＝スライド動画）

**バージョン**: 0.2（主要パラメータ確定 → §7。残タスクは §8）
**作成日**: 2026-08-15（v0.2 同日）
**前提**: MVP 完了後（チケット23＝Google Play リリース後）に着手する。
MVP の仕様は REQUIREMENTS.md v1.0 が唯一の情報源であり、本書は Phase 2 の追加分のみを定義する。

---

## 1. 概要

### 1.1 何を作るか

**ギャラリーの写真と本人の声から「上映会」（スライド動画）を作る機能。**

書き手がギャラリーから好きな写真を選ぶと、写真が順にめくられ、
その写真のお題に残した**本人の録音音声がナレーションとして流れる**動画体験ができる。
体験の核3「写真が語る」を、1枚ずつのタップから**ひと続きの上映**へ広げるもの。

- **機能の呼び名は「上映会」**（2026-08-15 確定）。劇場の世界観・幕演出と一直線で、
  シニアに馴染みのある言葉（公民館の映画上映会）
  - UI 文言例：「上映会をつくる」「上映会を見る」「上映会がはじまります」
- 語彙は MVP と同じ制約に従う（「終活」「メモリアル」等の葬送系語彙は禁止。REQUIREMENTS §1.1）

### 1.2 二段構え（重要）

| 段階 | 内容 | 実体 |
|---|---|---|
| **2a. アプリ内の上映会** | 選んだ写真＋声＋BGM を順に再生する画面。アプリと閲覧 Web の両方で見られる | 再生リスト（DB の JSON）。動画ファイルは作らない |
| **2b. MP4 書き出し** | 2a と同じ内容を MP4 ファイルにして、端末保存・家族への共有を可能にする | サーバー側 ffmpeg（Cloudflare Containers）で生成し R2 に保存 |

2a の再生リストが 2b のレンダリング指示書（設計図）を兼ねる。**2a を先に完成させ、2b はその上に載せる。**
2a の時点でユーザー価値が完結するため、2b は独立して延期・中止できる。

---

## 2. スコープ

### 2.1 Phase 2 に含める

- 写真の選択（5枚以上・合計10分まで）→ 上映会の作成・上書き保存
- 章立て再生（お題単位。§3.2）＋ BGM
- アプリ内・閲覧 Web での上映会再生（2a）
- MP4 書き出しと端末ダウンロード（2b）

### 2.2 Phase 2 に含めない（実装しないこと）

- 写真1枚ごとの個別録音（録音は引き続き**お題に1本**。データモデルを変えない）
- 上映会の複数本保存・履歴（**常に1本だけ。作り直すと上書き**）
- 動画編集機能（トリミング・テロップ・フィルタ等）
- 写真ごとの表示秒数の手動調整
- BGM の選曲機能（**1曲固定**。曲を差し替えるのは開発側の作業）
- テンプレート選択・エフェクト選択（演出は1種類に固定）

---

## 3. 機能要件

### 3.1 写真の選択

- ギャラリー（机の上）から選ぶ。**最低5枚**
- **声あり・声なしのお題の写真、どちらも選べる**
- 選択中に**できあがりの長さをリアルタイム表示**する（例：「できあがりは やく 4分」）
  - 算出：選択写真が属するお題の `recordings.duration_sec` の合計 ＋ 声なし写真の枚数 × **5秒**
  - シニアに枚数の逆算をさせない。「長くなってきた」が見えることが目的
- **上限は合計10分**。10分に達したらそれ以上選べなくし、やさしく案内する
  （例：「これいじょうは えらべません（10分まで）」）
- 作成実行時、**既に上映会がある場合は上書き確認**を出す
  （「いまの上映会は消えて、新しい上映会にかわります」）

### 3.2 章立てルール（本機能の核。決定済み）

録音は「お題に1本」なので、動画は**写真単位ではなくお題単位の章**で構成する。
これにより「同じ声が写真の枚数だけ繰り返される」ことは構造上起きない。

1. 選んだ写真を `answer_id` でグループ化し、1グループ＝1章とする
2. **章の並び順＝写真を選んだ順**（その章の写真が最初に選ばれた時点の順。2026-08-15 確定）。
   章内の写真の順番も選択順
3. 章の再生中、その章の録音を**1回だけ**流す
4. 章に写真が複数あるときは、録音の長さ ÷ 枚数 で均等にめくる（クロスフェード等の単純な切替）
5. 録音のないお題の写真は、1枚あたり**5秒**で表示する（2026-08-15 確定）

### 3.3 BGM（あり。2026-08-15 確定）

- **著作権フリー音源1曲を固定で使う**。音源はユーザーが選定して支給する（AI 生成は使わない）
  - ライセンス条件：商用利用可・アプリ/動画への同梱（再配布）可・クレジット表記の要否を確認すること
- 流し方（仮決め。実装時に耳で最終調整）：**全編に小さく流し、声のある章は BGM 音量をさらに下げる**
  （ダッキング。仮: 通常 30% → 声の間 15%）
- 音源ファイルは静的アセットとして持つ：アプリ＝同梱アセット／閲覧 Web＝public 配下／
  2b＝コンテナイメージに同梱（R2 経由にしない。署名 URL の原則と無関係な固定素材のため）

### 3.4 上映会の再生（2a）

- **アプリ**：専用画面で再生。全画面・写真は空背景＋ポラロイド調（DESIGN.md のトーンを踏襲。詳細は DESIGN 改訂時に確定)
- **閲覧 Web**：`/w/[slug]` 配下に再生ページ。家族はアプリなしで見られる
  - ブラウザの自動再生は拒否されうるため、**再生開始はユーザーのタップ**（チケット21 で確立済みのパターンを踏襲）
  - 署名 URL はキャッシュしない原則を維持
- 声と BGM の2系統同時再生（アプリ＝expo-audio のプレイヤー2つ／Web＝audio 要素2つ）
- 音声はオンライン前提（オフライン時は再生不可の案内。チケット19 の方針と同じ）

### 3.5 MP4 書き出し（2b）

- 作成の入口は 2a の完成画面（例：「動画ファイルにする」）。生成は**非同期**
  - 生成中はステータス表示（ポーリング。プッシュ通知は Phase 2 でも作らない）
  - 生成完了後：アプリ＝端末に保存（共有シートで LINE 等へ）／閲覧 Web＝「動画をダウンロード」ボタン
- **R2 キーは固定**：`subjects/<subject_id>/slideshow.mp4`。生成のたびに同キーへ上書き＝「常に1本」を保存構造で保証
  - アカウント削除の wipe は `subjects/<id>/` の prefix 一括削除（チケット18）なので、**動画も自動的に削除対象になる**（追加実装不要）
- ダウンロードは worker の署名付き URL 経由（既存原則どおり。直リンク禁止）
- **レート制限：1日3回/ユーザー**（2026-08-15 確定）。AI 生成と同じ枠組み（KV・JST 0時リセット）

---

## 4. 非機能要件

- **動画の向き・解像度：横 16:9・1280×720**（2026-08-15 確定）
- **写真は切り抜かない**：縦写真・横写真とも**全体を表示**（contain）し、余白は空グラデ背景で埋める。
  ギャラリーのポラロイドは正方形に切り抜いて見せているが、上映会は「写真そのものを見せる」場なので方針を変える
- **容量**：保存する動画は subject あたり最大1本（10分・720p・H.264+AAC で 100〜150MB 目安）
- **生成時間**：シニアが不安にならない見せ方をする（「できるまで 5分くらいかかります。できたら ここに出ます」等、画面を離れてよいことを明示）
- **写真の画質**：アップロード写真は長辺 1600px JPEG（チケット09）なので、720p の動画素材として追加処理なしで足りる
- **失敗時**：生成失敗はやさしい日本語で案内し、前回の動画があれば**消さずに残す**（新しい生成が成功してから置き換える）

---

## 5. 技術構成（MVP からの追加・変更）

### 5.1 MP4 生成基盤：Cloudflare Containers ＋ ffmpeg（2026-08-15 調査済み）

| 項目 | 内容 |
|---|---|
| 必要プラン | **Workers Paid（$5/月）が必須**。Phase 2 で新たに発生する唯一の固定費 |
| インスタンス | `basic`（1/4 vCPU / 1 GiB / 4 GB disk）で開始し、検証で不足なら `standard-1`（1/2 vCPU / 4 GiB / 8 GB） |
| 無料込み枠 | 月 25 GiB時（メモリ）・375 vCPU分・200 GB時（ディスク）。**個人利用の生成頻度なら込み枠内に収まる見込み**（1回10分の生成を月30回行ってもメモリ約 7 GiB時・CPU 約 150 vCPU分） |
| 課金モデル | 起動中のみ 10ms 単位課金。ジョブ完了で即停止すれば待機コストなし |
| 制限 | イメージ合計 50 GB/アカウント（ffmpeg 入りイメージは数百 MB で問題なし）。SIGTERM 後 15 分で強制終了 → 長尺動画の生成時間上限として意識する |

- worker が R2 から写真・音声を取り出してコンテナに渡し、ffmpeg で合成 → R2 の固定キーへ書き戻す
- **実装前に技術検証チケットを必ず置く**（MVP のチケット00 と同じ流儀）：
  Containers 上で ffmpeg が動くこと／10分動画の生成所要時間／`basic` で足りるか／
  BGM ダッキングのフィルタ構成／wrangler 設定

### 5.2 worker の役割（改定）

REQUIREMENTS.md §5「この2つ以外を持たせない」を Phase 2 で改定し、**3つ**にする：

1. AI 生成プロキシ（変更なし）
2. メディアの門番（変更なし）
3. **上映会（MP4）の生成ジョブ管理**：JWT 検証 → レート制限 → Containers 起動 → 完了を DB に反映

これ以外は引き続き持たせない。

### 5.3 データモデル（案）

```
slideshows          … 上映会（subject 毎に1本 = unique）
  id, subject_id (unique), items(jsonb), total_duration_sec,
  video_status('none'|'generating'|'ready'|'failed'),
  video_generated_at, updated_at
```

- `items` は章立て済みの再生リスト（jsonb）。章・写真とも**選択順を保存した時点で確定**させる：
  `[{ answer_id, recording_r2_key|null, photos: [photo_id, ...] }, ...]`
  保存時のスナップショット。再生時に photo が削除済みならその写真だけスキップする
- `video_r2_key` カラムは持たない（固定キーのため導出可能。ステータスだけ持つ）
- RLS は他テーブルと同じ（本人＝全操作／登録家族＝SELECT のみ）。閲覧 Web は service role で読む

### 5.4 実装順の目安（チケット分割イメージ）

```
P2-00  技術検証：Containers + ffmpeg（→ §5.1 の検証項目）
P2-01  slideshows テーブル・マイグレーション
P2-02  アプリ：写真選択 UI（5枚以上・10分上限・時間表示・上書き確認）
P2-03  アプリ：上映会再生画面（BGM 込み。2a 完成）
P2-04  閲覧 Web：上映会再生ページ
P2-05  worker：生成ジョブ API ＋ Containers 連携（2b）
P2-06  アプリ・閲覧 Web：MP4 保存/ダウンロード導線
```

P2-04 まででいったんリリース可能（2a）。P2-05 以降は独立に判断できる。

---

## 6. 画面一覧（Phase 2 追加分）

### アプリ（Expo）

1. **写真えらび**：ギャラリーの写真をタップで選択（チェックマーク）。下部に「やく◯分」＋「上映会をつくる」ボタン
2. **上映会**：全画面再生・とじるボタン。完成画面から「動画ファイルにする」（2b）

### 閲覧 Web（Next.js）

1. **上映会ページ**：タップで再生開始。2b 完了後は「動画をダウンロード」

---

## 7. 決定済み事項（再検討しない）

| # | 決定 | 決定日 |
|---|---|---|
| 1 | 章立て方式（お題単位・録音は章につき1回） | 2026-08-15 |
| 2 | 声あり・声なし両方の写真を選択可 | 2026-08-15 |
| 3 | 上映会は常に1本・作り直しは上書き | 2026-08-15 |
| 4 | 最低5枚から・合計10分まで | 2026-08-15 |
| 5 | 2a（再生リスト）→ 2b（MP4）の二段構え | 2026-08-15 |
| 6 | MP4 生成はサーバー側（Cloudflare Containers）。ffmpeg-kit は開発終了・Expo Go で検証不可のためネイティブ生成は不採用 | 2026-08-15 |
| 7 | 声なし写真は1枚5秒 | 2026-08-15 |
| 8 | 並び順は選択順（章・章内とも） | 2026-08-15 |
| 9 | BGM あり。フリー音源1曲固定・ユーザーが選定（AI 生成は使わない） | 2026-08-15 |
| 10 | 横 16:9・1280×720。写真は切り抜かず全体表示 | 2026-08-15 |
| 11 | 生成レート制限は 1日3回/ユーザー | 2026-08-15 |
| 12 | 機能の呼び名は「上映会」 | 2026-08-15 |

## 8. 残タスク・仮決め（実装着手前に確定する）

| # | 項目 | 状態 |
|---|---|---|
| 1 | BGM 音源の選定 | ユーザー作業。ライセンス（商用利用可・同梱可・クレジット要否）を確認して支給 |
| 2 | BGM の音量バランス | 仮: 通常 30%・声の間 15%。実装時に耳で調整 |
| 3 | 上映会画面の見た目の詳細 | DESIGN.md の Phase 2 改訂で確定（空背景＋ポラロイド調の方向のみ決定） |
