# ワタシアター REQUIREMENTS.md

**バージョン**: 1.0（MVP要件定義）
**作成日**: 2026-08-03
**対象**: Claude Code 実装引き継ぎ用

---

## 1. プロジェクト概要

### 1.1 ワタシアターとは

**「自分の博物館（劇場）を作る」アプリ。**

生きている本人が、固定のお題に答える形で自分のエピソード・写真・声を残し、
AIがそれらを紡いで「自分史」を生成する。完成した博物館は、URLひとつで
家族（孫世代）にWebで共有できる。

- 名前の由来：**ワタシ ＋ シアター**（「シ」で連結した合成語）
- 主表記はカタカナ「**ワタシアター**」。英字 *WATASHI THEATER* はロゴ装飾のみ
- **「終活」という言葉はアプリ内・ストア説明文で一切使わない**（重要な設計判断）
  - 位置づけはあくまで「自分の博物館づくり」「思い出の上映」
  - 「メモリアル」「追悼」「遺す」等の葬送系語彙も禁止

### 1.2 体験の核（3つ）

1. **問いかけが先にある**：白紙に書かせない。固定10問＋自由1枠のお題に答えるだけ
2. **AIが自分史を紡ぐ**：答えた分だけを素材に、AIが読み物としての「自分史」を1本生成
3. **写真が語る**：ギャラリーの写真をタップすると、本人の声でその話が流れる

### 1.3 ターゲット

- **書き手（主役）**: 60〜80代のシニア。スマホ操作は初級〜中級
- **見る人**: 書き手の家族。特に孫世代（10〜40代）。アプリ不要、URLをブラウザで開くだけ
- 一般公開アプリ（Google Play）。ほほ笑みラボは最初のユーザー獲得・実地テストの場

---

## 2. スコープ

### 2.1 MVP（本書の対象）

| 領域 | 内容 |
|---|---|
| 認証 | Google OAuth（Supabase Auth） |
| お題・回答 | 固定10問＋自由1枠。テキスト＋音声入力。写真添付。**全問任意** |
| AI自分史生成 | 回答済みのお題を素材に1回生成（再生成可、レート制限あり） |
| ギャラリー | 机の上メタファー（自由配置ボード）。デフォルトは自動整列 |
| 写真が語る | 写真タップで本人の録音音声を再生 |
| 共有 | ①招待コードで家族登録（「みたよ」リアクション可）②閲覧専用URL（見るだけ） |
| 閲覧Web | Next.js製。アプリ不要でスマホブラウザ閲覧 |

### 2.2 MVPに含めない（Phase 2 以降。実装しないこと）

- ペットの博物館（ただしデータモデルに `subject_type` のみ用意しておく → §6）
- 課金・収益機能
- コメント機能（文字でのやり取りは一切なし）
- プッシュ通知
- iOS版
- AIとの会話型聞き書き（MVPは「回答→1回生成」のみ）

**注意：Phase 2 に向けた先回り実装・抽象化をしないこと。**
`subject_type` カラムの存在以外、ペットや課金を意識したコードを書かない。

---

## 3. 機能要件

### 3.1 認証・オンボーディング

- Google OAuth のみ（メール認証なし）
- 初回起動時：3枚以内のかんたん説明 → ログイン → ニックネーム登録 → ホームへ
- ニックネームは後から変更可

### 3.2 お題と回答

#### 固定10問（この文言・順序で実装。文言はDB管理とし将来変更可能に）

1. 生まれた場所と、子どものころの話
2. 夢中になったこと
3. 仕事の話
4. 忘れられない事件・失敗談
5. 好きな食べ物
6. 大切な人との出会い
7. 自慢の一枚（※写真添付を促すお題）
8. 今の楽しみ
9. 若い人に言いたいこと
10. まだ誰にも話していないこと

#### 自由お題（1枠）

- ユーザーが自分でお題タイトルを付けて回答できる枠を **1つ** 用意
- MVPでは1枠のみ。複数化しない

#### 回答の仕様

- **すべて任意回答**。未回答でも他機能は使える
- 入力手段：テキスト／**音声入力（端末標準の音声認識）**の両方
- **音声回答時は録音データ自体も保存する**（→「写真が語る」の素材になる）
  - 録音の保存は都度ユーザーに明示（「この声は写真の説明として使えます」）
- 各お題に写真を複数枚添付可（上限：1お題あたり5枚）
- 回答は後から何度でも編集可

### 3.3 AI自分史生成

- ホームに「じぶん史をつくる」ボタン。回答が **1問以上** あれば実行可
- 回答済みのお題テキストをまとめて1回のAPI呼び出しで送信し、
  読み物としての自分史（縦書き風の随筆調、2,000字程度）を生成
- **回答カードの本文はAIが書き換えない**（本人の言葉のまま残す）。
  生成されるのは独立した「自分史」ページ1本のみ
- 再生成可。ただし **1日3回まで**（占いキッカケと同じ Cloudflare Workers 側レート制限、JST 0時リセット）
- 生成結果はユーザーが手動編集可能。編集後の再生成時は上書き確認ダイアログを出す
- AI呼び出しは必ず Cloudflare Worker 経由（APIキーをクライアントに置かない）

#### 生成プロンプトの方針（Worker側に実装）

- 一人称は本人。回答にない事実を創作しない
- 未回答のお題には触れない
- 温かいが感傷的すぎないトーン。「人生の終わり」を連想させる表現の禁止

### 3.4 ギャラリー（差別化の中核）

#### ① 机の上メタファー

- 全お題に添付された写真を1枚のボード（机）上に表示
- **デフォルトは自動整列**（グリッドではなく、少しランダムに傾き・重なりのある「ばら撒き風」自動レイアウト。シードを保存し毎回同じ配置）
- 「ならべかえ」ボタンで並べ替えモードに入った時だけドラッグ可能
  - ドラッグで位置移動、写真ごとに x, y, 回転角, 重なり順を保存
  - 「もとにもどす」で自動整列に戻せる
- 写真はポラロイド風の白フチ。キャプション（お題タイトル）を下部に表示
- **額縁スロット**は実装しない（Phase 2 候補として却下済み。作らないこと）

#### ② 写真が語る

- 写真をタップ → 拡大表示 ＋ その写真が属するお題の **本人の録音音声を再生**
- 録音がないお題の写真は、拡大＋テキストエピソード表示のみ
- 音声付き写真には小さなスピーカーアイコンを表示（見る側が気づけるように）
- 閲覧Web側でも同じ再生体験を提供する（HTML5 audio）

### 3.5 共有（C案）

#### (a) 登録家族（招待コード方式）

- 書き手が6桁英数の招待コードを発行（「あのね。」と同方式）
- 家族はアプリからコード入力で「家族」として登録（家族もGoogle OAuth）
- 家族ができること：閲覧＋**「みたよ」リアクションのみ**（写真・自分史単位で1タップ）
- 書き手側に「〇〇さんが みたよ しました」の一覧表示（アプリ内のみ。通知は出さない）
- コメント・文字入力は一切なし

#### (b) 閲覧専用URL

- 書き手が「見せる用リンク」を発行（推測不能なランダムスラッグ、例: `/w/xxxxxxxxxxxx`）
- リンクを知っていれば **登録不要・ブラウザだけ** で閲覧可（LINEで孫に送る想定）
- 閲覧のみ。リアクション不可
- 書き手はリンクを **無効化＋再発行** できる
- 発行時に注意文を表示：「このリンクを知っている人はだれでも見られます」

### 3.6 閲覧Webページ（Next.js）

- 構成：表紙（名前・代表写真）→ ギャラリー（机の上ボードの閲覧版）→ 自分史 → お題カード一覧
- 机の上メタファーのレイアウト（座標・傾き）をWeb側でも再現
- 写真タップで音声再生（§3.4②）
- レスポンシブ（スマホ最優先）
- 検索エンジンには載せない：**全ページ noindex**（いとぐちと同方針）

---

## 4. 非機能要件

### 4.1 シニアUX原則（ほほ笑みラボ標準）

- 文字サイズ大、高コントラスト、タップターゲット48dp以上
- 1画面1目的。深い階層を作らない
- 破壊的操作（削除・リンク無効化）は必ず確認ダイアログ
- **通知ゼロ**（プッシュ通知を実装しない）
- 専門用語禁止：「アップロード」→「のせる」、「生成」→「つくる」等、UI文言はやさしい日本語

### 4.2 オフライン・パフォーマンス

- 書き込みはオンライン前提
- 一度読み込んだ自分の博物館（回答・自分史・写真）は端末にキャッシュし、オフラインでも閲覧可（お医者さんナビ方式）
- 写真はアップロード前にクライアント側で圧縮（長辺1600px / JPEG品質80。「あのね。」方式）
- 音声は AAC、1録音あたり最長3分

### 4.3 プライバシー・安全

- 閲覧URLスラッグは十分に長いランダム文字列（推測不能）
- R2の写真・音声への直リンクは不可。必ずWorker発行の**署名付きURL（有効期限付き）**経由
- アカウント削除機能（本人データ・写真・音声の完全削除）をMVPに含める

---

## 5. 技術構成

| 層 | 技術 | 備考 |
|---|---|---|
| モバイルアプリ（書き手） | Expo（React Native）/ TypeScript | Android先行、Google Play |
| 閲覧Web | Next.js / TypeScript / Tailwind | Vercel。noindex |
| 認証・DB | Supabase（Auth: Google OAuth / Postgres / RLS） | Storageは使わない |
| 写真・音声ストレージ | **Cloudflare R2** | 転送料無料 |
| API中継 | **Cloudflare Worker** | 役割は下記2つのみ |
| AI | Gemini（Worker経由） | 自分史生成 |

### Cloudflare Worker の役割（この2つ以外を持たせない）

1. **AI生成プロキシ**：Supabase JWT を検証 → Gemini 呼び出し → レート制限（1日3回/ユーザー、JST）
2. **メディアの門番**：Supabase JWT（または閲覧スラッグの正当性）を検証 → R2 の署名付きURL発行（アップロード用／閲覧用）

---

## 6. データモデル（概要）

> 詳細スキーマは実装時に確定。ここでは構造の意図だけ固定する。

```
subjects            … 博物館の主役
  id, owner_user_id, subject_type('self')  ← MVPでは 'self' 固定。
                                              ペット対応(Phase 2)のためのカラムのみ用意
  nickname, cover_photo_id, board_seed

prompts             … お題マスタ（固定10問。文言をDBで管理）
  id, sort_order, title, is_photo_prompt

answers             … 回答カード
  id, subject_id, prompt_id(null=自由お題), custom_title,
  body_text, updated_at

recordings          … 録音
  id, answer_id, r2_key, duration_sec

photos              … 写真
  id, answer_id, r2_key, board_x, board_y, board_rotation, board_z
  （board_* が null なら自動整列）

life_story          … AI自分史（subject毎に1本）
  id, subject_id, body_text, generated_at, edited_by_user(bool)

family_members      … 登録家族
  id, subject_id, member_user_id, joined_at

invite_codes        … 招待コード
  id, subject_id, code, expires_at, used_by

view_links          … 閲覧専用URL
  id, subject_id, slug, is_active

reactions           … みたよ
  id, member_id, target_type('photo'|'life_story'), target_id, created_at
```

- RLS：本人=全操作可／登録家族=対象subjectのSELECT＋reactions INSERTのみ
- 閲覧URLアクセスはWebサーバー側（Next.js API Route）で slug 検証して読み取り（anonキーでの直接読み取りをさせない）

---

## 7. 画面一覧（MVP）

### アプリ（Expo）

1. **オンボーディング／ログイン**
2. **ホーム**：進み具合（「10このうち 4つ こたえました」）／じぶん史をつくるボタン／ギャラリーへ
3. **お題一覧**：固定10問＋自由1枠のカード。回答済みに印
4. **回答画面**：テキスト／音声入力切替、写真添付、保存
5. **ギャラリー（机の上）**：閲覧モード⇄ならべかえモード、写真タップで拡大＋音声
6. **じぶん史**：生成・閲覧・編集・再生成
7. **みんなに見せる**：招待コード発行／閲覧URL発行・無効化／家族一覧／みたよ一覧
8. **せってい**：ニックネーム変更、アカウント削除

### 閲覧Web（Next.js）

1. `/w/[slug]`：表紙 → ギャラリー → じぶん史 → お題カード一覧（1ページ構成でも可）

---

## 8. Claude Code への実装上の注意

- **MVPスコープを厳守。** §2.2 の項目・却下済み機能（額縁スロット、通知、コメント等）を実装しないこと
- 将来の拡張を見越した抽象レイヤー・設定項目を勝手に追加しないこと（`subject_type` カラムの存在のみが唯一の例外）
- UI文言はすべて日本語・やさしい言葉。本書の文言例（「みたよ」「ならべかえ」「じぶん史をつくる」）をそのまま使う
- デザイン詳細は別途 DESIGN.md にて定義（本書には含めない）
- 迷ったら「シニアの書き手が一人で迷わず使えるか」を判断基準にする

---

## 9. 用語集

| 用語 | 意味 |
|---|---|
| 博物館（シアター） | 1人の主役（subject）の回答・写真・音声・自分史の全体 |
| お題 | 固定10問＋自由1枠の質問 |
| 回答カード | 1つのお題への回答（本文＋写真＋録音） |
| じぶん史 | AIが回答を素材に生成する読み物（1本） |
| 机の上 | ギャラリーの自由配置ボード |
| みたよ | 登録家族による1タップリアクション |
| 見せる用リンク | 閲覧専用URL（登録不要） |
