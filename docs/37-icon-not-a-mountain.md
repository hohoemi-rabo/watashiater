# 37. アプリアイコン：「山に見える」を直す

- ステータス: 進行中（生成ずみ／ユーザー確認まち）
- 参照: DESIGN.md §1（マチネ）/ §6.1（開幕）。実装は `app/scripts/gen-icons.mjs`（本番アイコンの正）
- 依存: なし。**35（製品版申請）より先に決めること**（アイコンはビルド入力なので、35 で作る production ビルドに載せる）
- 由来: クローズドテストの声（2026-09-12「アイコンが山に見える」。ほほ笑みラボの生徒さん）

## 目的

アイコンが劇場の舞台ではなく**山の稜線**に見える、という声。色と中央のスポット光は問題ないので、
**形だけ**直す。

## 何が山に見えていたか

1. **房飾りの弧が上向きの角（つの）を作っていた**：弧を5つ並べていたので、弧と弧のあいだに
   上向きの尖りができ、それが山の頂に読めた
2. **左右の幕が裾に向かって大きく外へ開いていた**（`panelInnerEdge` が `0.34 → 0.10`）：
   その斜めの輪郭が山の斜面になった
3. **幕の面がのっぺりしていた**：縦のひだが ±7% の淡い濃淡しかなく、布ではなく「塗られた斜面」に見えた
4. 結果、中央の空グラデが「山あいの空」、下の木の床が「地面」として読めてしまった

## 最終的にどうしたか（2026-09-12）

まずコード側で形を直した（下記「コードで直したこと」）。それを見たユーザーが**自分で緞帳の絵を用意**し、
そちらを採用することにした。**アイコンの作り方そのものが変わった**：

- 元絵は `app/assets/icon-source.jpg`（ユーザー提供・2048px）。差し替えるときはここを置き換える
- `scripts/gen-icons.mjs` は**元絵を各サイズに切り出すだけ**になった。1ピクセルずつ描いていた
  従来のコード（依存なし・決定的生成）は役目を終えたので捨てた
- 切り出しに `sharp` を使うので、app の devDependencies に入れた（アプリには積まれない）

### 切り出しで気をつけたこと

- **アダプティブアイコンで実際に見えるのは 108dp のうち中央 72dp（66.7%）だけ**。角が丸く落ちる
  だけではない。foreground には絵を全面ではなく中央 66.7% に収めた。円マスク・角丸マスクの両方で
  絵が欠けずに収まることを、実際にマスクをかけて目視した
- 出力は **256色パレット PNG・dither なし**。写真的な絵を素の PNG にすると 1024px で 2MB 近くなり、
  アイコンとスプラッシュだけでアプリが数 MB 重くなる。パレット化で6分の1になり、アイコンとして
  出る大きさでは劣化が分からない。dither を入れると**スポット光のような滑らかな面に点ノイズ**が出た
- 段階の粗さが見えるのは 1024px で見たときだけなので、**大きく出るストアのバナーだけフルカラー**
  （バナーはアプリに積まれない）
- スプラッシュは 200dp 幅で出す設定なので 512px にした（1024 は無駄）
- monochrome（テーマアイコン）は写真的な絵から作れないので、幕のシルエットを SVG で別に描いた

## コードで直したこと（絵を差し替える前。色・光・構図の要素はそのまま）

- **房飾りの下端をほぼ水平に**（波の振幅 0.075 → 0.018）。上向きの角を作らない
- **左右の幕をほぼ垂直に吊るす**（`0.2 + 0.04·y²`。裾だけわずかに外へ広がる）。
  幕は「吊るされて垂れているもの」なので、縦に落ちる輪郭のほうが幕らしい
- **縦のひだをはっきり付ける**（±7% → ±16%）。布に見えれば斜面には見えない
- **房飾りを少し明るく**（×1.12）して、左右の幕と地続きに見せない
- **内側の縁を金線に**（淡い桜色 → `#E3AD4E`）。アプリ内の緞帳（チケット29）と同じ
  `curtain-gold` で、劇場の緞帳だと一目で分かる印にする。桜色は地に溶けて輪郭を作れていなかった
- 開口部を広げた（幕の占有が減り、舞台と床・スポット光が見えるようになった）

## Todo

- [x] `gen-icons.mjs` の形の修正（房飾り・幕の輪郭・ひだ・金線）
- [x] `node scripts/gen-icons.mjs` で全サイズ再生成（生成物はコミットする）
- [x] 小さい表示（192/96/72/48px）とアダプティブ foreground で目視
- [x] ユーザー提供の絵（`icon-source.jpg`）へ差し替え、`gen-icons.mjs` を切り出し方式に作り直し
- [x] アダプティブの安全領域（中央66.7%）・円/角丸マスクでの見え方を実測
- [ ] ユーザー確認（大小の見え方・ストアのバナー）
- [ ] Play Console の掲載アイコンとフィーチャーグラフィックの差し替え（**35 に含める**。ビルド不要）

## 完了条件

ホーム画面の小さい表示で「幕の開いた舞台」と読め、山には見えない。ユーザーの了承を得ている。

## メモ

### ビルドとの関係

アイコンは**ネイティブのビルド入力**なので、Play のアプリに反映するには新しいビルドが要る。
このアプリには OTA 更新（expo-updates）が無く、**29〜36 の修正も新しいビルドでしか届かない**ため、
どのみち 35 で production ビルドを1回作る。アイコン変更はそれに載せれば追加のビルドは要らない。
Play Console 側の掲載アイコン（512×512）とフィーチャーグラフィックはビルド不要で差し替えられる。

### PWA 側

`public/icons/*` も同じスクリプトが作るので、push すれば書き手Web（PWA）のアイコンは
ビルドなしで新しくなる。ホーム画面に追加ずみの端末は、追加し直すまで古いアイコンのままになる。
