# 17. 共有：閲覧専用URL

- ステータス: 未着手
- 参照: REQUIREMENTS.md §3.5(b) / §4.3（推測不能スラッグ）
- 依存: 04（表示先の閲覧Webはチケット20・21）

## 目的

登録不要・ブラウザだけで見られる「見せる用リンク」の発行・管理を作る。

## Todo

- [ ] 「見せる用リンクをつくる」：推測不能なランダムスラッグで `view_links` を発行（例: `/w/xxxxxxxxxxxx`）
- [ ] 発行時に注意文を表示：「このリンクを知っている人はだれでも見られます」
- [ ] 無効化＋再発行（無効化は破壊的操作なので確認ダイアログ）
- [ ] リンクの共有（OSの共有シート。LINE で孫に送る想定）

## 完了条件

リンクの発行・無効化・再発行が動き、無効化したスラッグでは閲覧できない状態が DB 上で確認できる。

## メモ

（作業中の記録）
